{
  "name": "everest-restaurant",
  "version": "1.0.0",
  "description": "Everest Restaurant Web Application",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm run preview"
  }
} 